# Security Notes

- Secrets are stored in environment variables.
- No credentials are committed to source control.
- Permissions are scoped to required Graph API actions only.
- Scripts are designed to be idempotent where possible.
